#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <arpa/inet.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <unistd.h>
#include <netdb.h>

int calc (char *s) {
  int result;

  // Calculate the result of the operation here

  return result;
}

int main (int argc, char **argv) {
  // Open connection

  // Switch to buffered IO

  // Loop over input lines
  // Calculate the result (with calc())
  // Send result back to server

  return 0;
}
